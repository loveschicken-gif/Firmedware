# Database
DATABASE_URL="postgresql://firmedware:firmedware@localhost:5432/firmedware"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET="change-me-in-production"
AUTH_URL="http://localhost:3000"

# Seed admin (only created when no users exist)
SEED_ADMIN_EMAIL="admin@firm.local"
SEED_ADMIN_PASSWORD="changeme"

# AI provider keys (fork / custom integration only — NOT used by core v1.2)
# Configure on the server or secret manager after written firm AI policy.
# See docs/AI_CONNECTOR_SETUP.md
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=
# GOOGLE_GEMINI_API_KEY=
# PERPLEXITY_API_KEY=
# LOCAL_AI_BASE_URL=
# LOCAL_AI_API_KEY=
